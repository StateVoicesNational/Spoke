import {getScriptFieldValue} from '../../src/lib/scripts'

describe('stripWhitespace', () => {
  it('should remove leading and/or trailing whitespace', () => {
    let test
    test = applyScript()
  })
})
