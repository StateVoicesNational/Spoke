import React from 'react'

//First, we pass in any props transferred to this component
export const SetupPage = (props) => {
    return <div>
      <AppBar title="Campaign setup"/>

      Setup!!!!
    </div>
}